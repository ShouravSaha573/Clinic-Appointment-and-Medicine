# Backend environment variables

# MongoDB
# Example: mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority
MONGO_URI=

# JWT secret
SECRET=super-secret-jwt-key-for-clinic-app

# Environment
NODE_ENV=development

# CORS
# For production deployments (e.g. Render), set this to your frontend origin.
# Example: https://your-frontend.onrender.com
# Multiple allowed origins can be comma-separated.
CORS_ORIGINS=

# Cloudinary (optional but recommended for profile pictures/medicine images)
# Create a Cloudinary account and copy these values from your Cloudinary dashboard.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
